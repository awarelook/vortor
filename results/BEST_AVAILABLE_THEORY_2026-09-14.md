# The Best Available Theory — crystallized, audited, and organized for the next breakthrough

**Author:** Nathaniel Hanks · **Date:** 2026-09-14 · **Provenance:** a six-dimension jewel audit
(completeness / coherence / derivation depth / explanation quality / sourcing / theoretical advancement),
each dimension run as an independent adversarial pass, findings verified line-by-line against the files,
then folded. Harness after the fold: `verify_all.py` → **89/89** (82nd: the torus expedition; 83rd: Stage D; 84th: the spin gate — all 2026-09-15).

This document does three things: (1) states the **real working theory** — what the jewel actually is when
every claim is held at its earned tier; (2) records the **audit outcome** including the forgotten-batch
capture (the user-named worry: past calculations not captured, or falsely ruled out); (3) organizes the
**breakthrough program** toward matter-wave synthesis, coherent energy transformation, and transmutation —
with the EVO/levitation fences stated plainly.

---

## 1. The working theory, in one tiered page

**The object.** One driven, force-free **Beltrami–Hopf toroidal soliton** (`curl B = λB`, single λ), read
simultaneously as a *field* (plasmoid/EVO scale) and a *matter wave* (particle scale). Four measured
anchors `{B, n_i, m_i, R}` plus one `[S]` geometric selection (`ε = a/R = 1/φ`); zero *fitted* structural
parameters.

**What is settled `[V]`/`[credited]` (each row a named, re-runnable check):**
- **Coherence IS regularity** — at the Beltrami state the Lamb vector `u×ω = 0` and the object is an exact
  eternal solution (the exact-solution class credited to Trkal 1919); driven, it is an exact steady state
  AND a stable attractor swept Re 126→628.
- **The CK carrier comb** — `tan x = x` roots → ratios `1 : 1.719 : 2.427` ({121, 208, 294} kHz at the
  anchors) — the spectral fingerprint, a *linear-limit* statement (M16), spherical-boundary geometry stated.
- **Heartbeat, not flywheel** — fixed-helicity minimization is an LP whose optimum is one mode: a
  beat-carrying object must be driven. The N-mode/Woltjer structure, Kuramoto onset (now in-repo,
  P = 1.20), and the Aizawa chaotic boundary (now in-repo, λ_max = 0.094 > 0) frame the *rhythm* layer.
- **Topology & chirality** — idealized-reference `Q_H = 1`, wave-mode Chern `C = ±2`; the object's own
  computed content: chirality = `sign λ`, mirror = C-structure, self-dual `θ_χ = 45°` = Majorana (algebra
  `[V]`; the QFT dictionary `[S]`). The actual CK object carries real `H ≈ 0.088`, not an integer charge.
- **The nuclear ledger** — `d+d→⁴He` = 23.847 MeV, conserved; COP > 1 is nuclear-sourced, never over-unity;
  transmutation Q-values (Cs→Pr 50.493, Sr→Mo 53.412 MeV) are now *harnessed* AME2020 arithmetic.
- **The disposal chain (LENR), converged** — the γ-quiet `0⁺→0⁺` disposal operator is **E0/collective**
  `[credited]` (anapole toroidal dipole double-forbidden); the anapole is the **active site**, not the
  antenna; neutrino/4d→2α/coherence-volume escapes each computed shut.

**The settled-negatives (kept as wins):** α-from-winding (ι≈1, not 137); the literal kHz=MeV identity
(dead 16.8 OOM, pump doubly-closed); direct phonon-nuclear coupling (~66 OOM); monopole catalysis;
TSC-class 4D→2α exits ((α,n)-excluded — a *named prior-art confrontation* with Takahashi's exit channel);
supra-threshold slow dwell (zeptosecond survival); sub-threshold state ladders (no bound excited ⁴He).

**The honest frontier `[S]`/open:** the matter-wave *identification* (electron = this object); the π₁/π₃
lock behind g=2 (meaning derived, lock diagnostic); α's value (typed, not derived); the mass hierarchy
(order derived, ratios preprint-tier); unconditional high-Re regularity (GPU run); and **the one nuclear
open problem** — §3 below.

---

## 2. The audit outcome (2026-09-14)

Six dimensions, six grades: completeness **B**, coherence **B**, derivation depth **B**, explanation
quality **B+**, sourcing **B+**, theoretical advancement **B+**. The through-line: *the harness-side
discipline is excellent* (75-then-78 scripts all mapped, zero orphan checks, zero dangling references,
counts consistent everywhere) *and the failures were concentrated in exactly the pattern the user feared* —
a batch of past calculations carried as `[V]` without in-repo capture, plus superseded framings surviving
in the long-form layer.

**Fixed this pass (all verified against the files before editing):**
- The stale "one computable matrix element" LENR framing in README, the plain-language brief,
  `paper/master.md`, the Grand Synthesis, `LENR_YINYANG`, and `coherence.html` panel 6 — all now carry the
  09-14 supersession (E0 operator / active site / assembly corridor).
- Tier divergences harmonized (C/Majorana: algebra `[V]` / dictionary `[S]` everywhere; Q_H idealized-
  reference qualifier at the front doors; g=2 brief bullet demoted to match the canon; density leg retagged
  `[V-consistency given the London ansatz]`; "zero free parameters" → "four anchors + one `[S]` selection").
- Under-claims corrected (the discipline cuts both ways): the current-leg trilogy and the S³ curl spectral
  zeta promoted into the MVP proven-core table; the TSC (α,n)-exclusion promoted to a named prior-art
  settled-negative.
- Sourcing: the ~1e-7 baseline primary resolved (Wilkinson & Cecil, PRC **31**, 2036 (1985)) and cited at
  the points of use; the phantom-citation keep-out note reworded so its own grep invariant holds; duplicate
  and ambiguous entries fixed; the Trkal credit added.
- **One audit finding REFUTED in the repo's favor:** an auditor claimed the Barnes–Baskerville–Turok B=4
  citation (PRL 79, 367) was a B=2 mix-up; verification against the original (hep-th/9704012) confirmed
  the repo was right. The challenge is logged in REFERENCES — the meta-lesson (verify literature claims
  before editing, in *both* directions) held.

**Meta-lesson repeated:** multi-lens audit + line-level verification caught overclaims, *under*-claims, and
one false accusation — the same triple the LENR disposal chain produced. The discipline is symmetric or it
is nothing.

## 3. The forgotten batch — captured, retagged, or backlogged

**Captured (new/extended checks, 78 → 81 on 09-14; → 84 on 09-15 — beat-tunneling 82nd, Stage D 83rd, the spin gate 84th):**
- `multibody_sync_capture_check.py` — the Grand Synthesis Part-D orphan `[V]`s recomputed: Kuramoto N=800
  Lorentzian onset **P = 1.20** (the synthesis's quoted ~1.2 reproduced), r(2K_c) = 0.69 (mean-field 0.71);
  Aizawa **λ_max = 0.094 > 0** (Benettin; literature band 0.10–0.124).
- `transmutation_qvalue_arithmetic_check.py` — the map's "[V]-arith, not harnessed" exception closed.
- `entrance_corridor_survival_check.py` — the frontier sharpener (below).
- `delta_b4_landau_zener_bridge_check.py` TEST 2b — the two-bar correction + a **new derived fence**.
- `COINCIDENCE_LEDGER` — the ω_EVO = 2πf₁ (0.035%) match logged **provenance-incomplete**; its `[V]`
  stripped in the synthesis pending a vendored source.

**Retagged honestly (computed in the parent corpus, not yet in-repo — `[V-external]` + backlog):**
`⟨C⟩ = |V_111| = 2.30` and `⟨D⟩ ≈ 0.86` (CK eigenmode overlaps — the comb-pull falsifier coefficient);
the Madelung continuity residual `~5e-16`; the full O_h gerade/parity settlement of `C_α`/`A_α`; the
grid-convergent `H = 0.088` of the actual CK object.

**The capture backlog, ranked by leverage (all tier-B feasible except where noted):**
1. **StageD BPS first-order ρ_eff bracket** — ~~"quadrature, not HPC"~~ **EXECUTED 2026-09-15
   (`delta_b4_stageD_bps_overlap_check.py`) and the scoping was itself half-wrong (the audit got
   audited):** on the exact ASW compacton the first-order **L₂ energy log-diverges at the compacton
   boundary** (increments = 2·ln10 per decade, computed) while **L₄ is finite** (11.886, compacton
   units) — so only the `c₄` piece is quadrature; the `c₂` piece needs boundary-layer regularization
   (research-grade). What WAS computed: the **density-geometry proxy bracket [0.55, 0.96]**
   (Cauchy–Schwarz-normalized Franck–Condon overlap of the B4 ball vs 2×B2 dumbbell at three crossing
   geometries, grid-converged) — which yields a **new structural constraint**: density-support geometry
   CANNOT supply the needed ρ_eff ~ 0.07; the smallness, if real, must originate in the
   **orientation / Finkelstein–Rubinstein phase structure** of the crossing (+ moduli metric + the
   regularized boundary). The external run should resolve the orientation-space structure *first*.
2. **CK overlap integrals** (`V_111`, `⟨D⟩`) — converts the comb-pull falsifier coefficient to in-repo `[V]`.
3. **EMF junction derivation** `V = ω_b·ΔΦ` vs LEC's 525 mV — needs one declared junction-geometry model
   (a new `[S]` choice), then is a ~50-line EM computation; currently an `[S]` order-of-magnitude.
4. **O_h with inversion** — extend `octahedral_oam_ladder_check` with the gerade/parity selection content.
5. **Madelung residual + H(CK) quadrature** — two small captures closing the last `[V-external]` tags.
6. **Beat-ladder validity-boundary theorems** (particle floor / instability ceiling / S_crit ~ 1e4) — the
   ark's A3 "MVVC ledger + scaling-theorem proof script", the one salvage fold still unexecuted.
7. **ω_EVO provenance** — vendor the source measurement or the flag stands.

**Falsely-ruled-out review:** the EXCISION_LEDGER cuts were re-read; every excision carries a *computed*
reason (`e^(-2/3)`→72 not 137; E_fm fabricated; COP never derived; Storti radii 10³–10⁴× off). Nothing
valuable was found wrongly killed. The one case of *over*-ruling found anywhere was the "too defeatist"
scoping already corrected (nonpopulation), plus the HPC over-scoping in backlog item 1.

---

## 4. The sharpened frontier (what changed today, structurally)

The one open problem was "the entrance-channel assembly geodesic, ≥3 open objects." Two in-env
computations collapsed it:

1. **The census** — ⁴He has **no bound excited states** (first excited 0⁺₂ at 20.21 MeV > p+t at
   19.815): there is *no state ladder* below breakup. 2. **The survival** — above p+t, Γ≈0.5 MeV kills any
   dwell in ~9×10⁻²² s: *slowness exists only sub-threshold*. Therefore the only aneutronic route is a
   **dissipative sub-breakup corridor**: the assembly must shed the full 23.85 MeV *concurrently with its
   own motion* on the near-BPS B=4 adiabatic surface, never pausing, never crossing above p+t.

It carries **two data bars**: *existence* — beat the measured ~1e-7 (Wilkinson–Cecil 1985); *sufficiency* —
the observed dearth `n/⁴He ≤ 1e-9`, i.e. `Γ_LZ ≥ ln(1e9)/2π = 3.30`, `Δ_suff = 5.47 × Δ_dominance`. And the
sufficiency bar plus the Cauchy–Schwarz ceiling `Δ ≤ 23.85 MeV` **derives a new constraint**:
`β·|dF| ≤ 0.874 MeV/fm` — *the data itself forces the crossing into the slow/soft corner*, converging
independently with the corridor result. One geometric question, two computable bars, decidable either way
(corridor excluded → the aneutronic reading falls — a decisive settled-negative, which is also a win).

**The gate (2026-09-15, `spin_channel_gate_check` — the push past Stage D).** Stage D localized the
suppression to orientation space; the gate check computes its quantum arithmetic: the s-wave door to
⁴He(0⁺) is the **singlet alone** (statistical gate 1/9 `[credited]`), and it is **steerable by coherent
preparation** — a single-mode condensate pair has singlet fraction **1/3 at m=0** and **exactly 0 at
m=±1** (d-wave residual ~10⁻⁶). Thermal polarization is ~10⁻⁶ at any plasmoid field, so channel selection
must be *dynamical/coherent* — the cold Bose seed's mechanism-shaped job. Logged (coincidence-class):
`1/9 × [0.55, 0.96] = [0.061, 0.107]` overlaps the target band 0.06–0.08 — three independent pieces at
one decade. **New falsifier #9: polarization-steered aneutronic yield** (m=±1 collapse is the sharp kill).
The pieces now interlock: *corridor (where) → slow/soft fence (how fast) → orientation gate (what selects)
→ coherent seed (what prepares) → polarization knob (how to test)*.

---

## 5. The organized breakthrough program (with its fences)

**A. Matter-wave synthesis** (the particle rung). The structure is earned: internal Dirac algebra, g=2's
meaning, chirality/C/Majorana algebra, kinematics simulated (`v_g v_p = c²`). The identification stays
`[S]`. The two in-env moves: a computed π₁–π₃ coupling on the eigenmode (would make the g=2 lock
predictive rather than diagnostic), and the mass-tower capture. The kill: `0νββ` null → Majorana falls.

**B. Coherent energy transformation** (heat → collective EM). The honest chain: nuclear-sourced energy
`[V]` → must shed collectively (E0 rule, `[credited]`) → *if* it sheds into the anapole's coherent
near-field mode, a reactive EMF `V = ω_b·ΔΦ` follows (`[S]`, backlog item 3 derives its magnitude) → the
LEC 525 mV and Mizuno 2025 EMF data are the confrontation set. The kill: no drive/beat dependence of the
EMF, or no correlation with heat.

**C. Transmutation** (baryon-conserving ΔA=4n). The arithmetic is now harnessed `[V]`; the mechanism
(lattice hops at a coherent site) is `[S]`; the rate is open behind the same corridor as (A)'s nuclear
rung. The discriminator: ¹⁶³Dy→¹⁶³Ho ionization-gated β⁻ by charge-state spectroscopy. Fence: baryon-
conserving transmutation ≠ baryon decay, always.

**D. EVO / SAFIRE / plasmoid engineering.** The strongest original `[V]` object (the driven Beltrami
attractor) plus the two spectral discriminators (CK inharmonic comb; `f_b(L) = N^L` yield steps — both
currently with ZERO experimental convergence, live targets). The kHz beat is a *rate-gate*, never a pump.

**E. The fences, stated once, plainly.**
- **No over-unity, ever.** COP > 1 is nuclear-sourced; the first law holds; FTGB derives no COP.
- **No levitation/thrust claim.** Nothing in the verified corpus computes a net force on or by the object
  beyond ordinary MHD/ponderomotive physics (and the ponderomotive *rate* lever is settled-negative). Any
  "EVO levitation/propulsion" reading is untiered speculation until someone computes a force balance —
  none is computed here, so none is claimed.
- **No new nuclear mechanism is demonstrated.** The program is *falsifiable architecture*: bars, fences,
  discriminators, and one named open computation.
- **No number is fabricated.** The open numbers (ρ_eff, the corridor, the EMF magnitude) are named as open.

---

## 6. Reproduce & falsify

`pip install -r requirements.txt && python results/verify/verify_all.py` → **89/89**, deterministic, no
network, CI-gated. The falsifier set: `FTGB_MINIMUM_VIABLE_PAPER.md` §5 (comb linear-limit; neutron∝heat
primary; E0-pair secondary; `N^L` beat-lock; 0νββ; the parameter-free scalars). The tier ledger:
`results/TIER_LEDGER.md`. The LENR map: `results/LENR_EXPLANATORY_RESOLUTION_MAP_2026-09-14.md`.

*No claim exceeds its tier; no number is fabricated; settled-negatives are kept as wins; the discipline is
symmetric (over-claiming and under-claiming are the same violation).*
